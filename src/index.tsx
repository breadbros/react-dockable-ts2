import Dockable from './dockable/Dockable';
import { Widget } from './dockable/Widget';
import { PanelState, WindowState, DockableProps } from 'types';

export default Dockable;
export { Widget, PanelState, WindowState, DockableProps };
